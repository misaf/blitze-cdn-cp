# Managed by BlitzeCDN (blitzecdn_controlplane). Manual edits are lost.
Host localhost 127.0.0.1
    IdentityFile {{ blitzecdn_controlplane_install_dir }}/.state/id_ed25519
    IdentitiesOnly yes
    StrictHostKeyChecking yes
    UserKnownHostsFile {{ blitzecdn_controlplane_install_dir }}/.state/.ssh/known_hosts
