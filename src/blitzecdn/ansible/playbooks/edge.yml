---
- name: Converge BlitzeCDN edge servers
  hosts: blitzecdn_edges
  become: true
  gather_facts: true
  # One failed edge stops the play. `max_fail_percentage` is deliberately not
  # set alongside this: it expresses the same policy in a second dialect, and
  # the two invite a later reader to change one and believe they have changed
  # the rule. `any_errors_fatal` is the stricter of the pair and wins anyway.
  #
  # It matters more now than it did, not less. A runtime upgrade rolls out one
  # batch at a time, and an image that fails to serve on the first batch must
  # stop the rollout rather than reaching the whole fleet — each failing edge
  # returns to its previous image on its own, but only the play can stop the
  # next batch from being handed the same broken build.
  any_errors_fatal: true
  # A canary, then widening batches. `any_errors_fatal` above is what makes the
  # first entry worth having: a runtime upgrade that fails to serve stops after
  # one edge instead of a quarter of the fleet, and each failing edge returns to
  # its previous image on its own while the play stops the next batch from being
  # handed the same broken build.
  #
  # The percentages are of the whole fleet, not of what is left, and Ansible
  # stops consuming the list once the hosts run out — so a two-edge fleet is
  # simply 1 then 1, and a single-edge fleet is one batch. Nothing here needs a
  # minimum fleet size.
  serial:
    - 1
    - "10%"
    - "25%"
    - "100%"
  pre_tasks:
    - name: Validate supported operating system
      ansible.builtin.assert:
        that:
          - ansible_facts.distribution == 'Ubuntu'
          - ansible_facts.distribution_version == '26.04'
        fail_msg: BlitzeCDN edges for this release require Ubuntu 26.04 LTS.
      tags: [always]
    # Establishes and validates the shared edge runtime contract: the host
    # paths, the public listener sets, the status endpoint, the runtime image
    # and the capability data directory that blitzecdn_nginx,
    # blitzecdn_edge_stack and blitzecdn_firewall all converge against. First, and before anything on
    # the host changes, so a contract no edge could serve fails a play that has
    # not yet touched the machine.
    #
    # This replaced a pairwise assertion here that blitzecdn_firewall's and
    # blitzecdn_nginx's HTTP/3 desired state agreed. There is one value now, so
    # they cannot disagree — and the contract asserts the part that assertion
    # could not: that 443 is actually an HTTPS listener for QUIC to attach to.
    - name: Establish the shared edge runtime contract
      ansible.builtin.import_role:
        name: blitzecdn_edge
      tags: [always, firewall, nginx, edge_stack]
    # The container engine, and then everything the edge runtime needs to exist
    # before a configuration is rendered: the persistent directories, the
    # runtime image and the Compose project.
    #
    # Both run before the firewall: opening the public ports on a host that
    # cannot serve them advertises an edge that is not there. The container
    # itself is not started here — that waits until the configuration exists and
    # has passed `nginx -t`, at the end of the roles below.
    - name: Install and verify the host container engine
      ansible.builtin.import_role:
        name: blitzecdn_docker
      tags: [always, docker]
    - name: Prepare the persistent edge state and runtime image
      ansible.builtin.import_role:
        name: blitzecdn_edge_stack
        tasks_from: prepare.yml
      tags: [always, docker, edge_stack]
    - name: Verify the edge runtime image before firewall changes
      ansible.builtin.import_role:
        name: blitzecdn_nginx
        tasks_from: verify-runtime.yml
      vars:
        # The digest prepare.yml just pinned, not the tag it was asked for. The
        # image that is probed has to be the image that will serve.
        blitzecdn_nginx_config_test_image: >-
          {{ blitzecdn_edge_stack_resolved_image
             | default(blitzecdn_edge_runtime.image) }}
      tags: [always, nginx]
  roles:
    - role: blitzecdn_base
      tags: [base]
    # Host DNS resolution is no longer named here. It ships as
    # `blitzecdn-resolver` and asks for the capability slot below, which is
    # still before blitzecdn_nginx renders the origin hostnames the runtime
    # will look up — so a fleet whose DNS belongs to its network detaches the
    # distribution instead of setting a variable in a role it never wanted.
    - role: blitzecdn_kernel
      tags: [kernel]
    # Every installed capability's own edge role, in one slot.
    #
    # The control plane composes the list from the plugins it discovered —
    # `blitzecdn_capability_roles` arrives as an extra-var on every run — so
    # this play names no capability and needs no edit when one is attached or
    # detached. An installation with none runs nothing here.
    #
    # The position is the contract. After the pre-tasks above, because a
    # capability role may need the container engine, the runtime image or the
    # persistent directories they establish; and before blitzecdn_firewall and
    # blitzecdn_nginx, because what a capability puts on an edge is something
    # the rendered configuration then depends on, and blitzecdn_nginx proves
    # the whole tree loads before the edge serves from it.
    - role: blitzecdn_capabilities
      vars:
        blitzecdn_capabilities_roles: "{{ blitzecdn_capability_roles | default([]) }}"
      tags: [capabilities, nginx]
    - role: blitzecdn_firewall
      tags: [firewall]
    # Renders the whole configuration tree onto the host filesystem and proves
    # it loads, under the very image the container will run. Nothing is started
    # here and nothing is reloaded unless a container is already serving.
    - role: blitzecdn_nginx
      tags: [nginx]
    # Starts the edge, or leaves a running one alone, or replaces it for a
    # runtime upgrade — and then proves it is serving. Strictly after the
    # configuration exists: a container started against an incomplete tree is
    # an edge answering 444 for every customer.
    - role: blitzecdn_edge_stack
      tags: [nginx, edge_stack]
    # The second capability slot: what an installed capability does to the
    # *host* rather than to the runtime, once the edge is already serving.
    #
    # This is where SSH policy and Fail2Ban run, and they are no longer named
    # here — `blitzecdn-hardening` ships both and asks for this slot, so a
    # fleet whose host access belongs to a golden image or a bastion detaches
    # the distribution instead of forking this play.
    #
    # The position is as much a contract as the edge slot's, and for a reason
    # that is nearly the opposite. A role here must come after the firewall,
    # because a host that fails firewall validation must never be left key-only
    # but unreachable from the management network; and after
    # blitzecdn_edge_stack, because an edge whose containers are all broken
    # must still be reachable for Ansible to repair it, so nothing that could
    # close the management path may run before the runtime has proved it
    # serves. Ordering *within* the slot is the contributing package's, which
    # is how Fail2Ban follows SSH without this play knowing either exists.
    - role: blitzecdn_capabilities
      vars:
        blitzecdn_capabilities_roles: >-
          {{ blitzecdn_host_capability_roles | default([]) }}
      tags: [capabilities, security]
