# blitzecdn-security

Optional site-level firewall and Under Attack implementation for BlitzeCDN.
The stable `SecurityPolicy` remains in the core package; default/disabled
security configuration does not require this package.
