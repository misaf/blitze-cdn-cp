# blitzecdn-certificates

Optional certificate upload, ACME issuance and renewal, managed certificate
deployment, and Automatic SSL/TLS for BlitzeCDN. TLS policy and existing edge
certificate paths remain stable core contracts.
