# Managed by BlitzeCDN. Local edits are overwritten.
#
# MaxMind credentials for the geoipupdate container. 0600 root-owned, delivered
# as an env_file rather than an `environment:` entry so the key never reaches
# the compose file, `docker inspect`, or any repository. Every task that
# touches this file sets no_log.
GEOIPUPDATE_ACCOUNT_ID={{ blitzecdn_geoip_account_id }}
GEOIPUPDATE_LICENSE_KEY={{ blitzecdn_geoip_license_key }}
