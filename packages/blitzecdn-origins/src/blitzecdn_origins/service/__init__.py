"""What this capability decides: how to ask the fleet, and how to read it back.

`checks.py` runs the play across the edges in scope; `reporting.py` turns one
host's raw report into the rows an operator sees. The same layer, so the same
rule — a service depends on contracts, never on a concrete adapter.
"""
