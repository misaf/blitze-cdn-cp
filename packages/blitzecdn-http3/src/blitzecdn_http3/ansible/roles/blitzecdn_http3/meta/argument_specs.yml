---
argument_specs:
  main:
    short_description: Validate package-owned HTTP/3 listener state
    options:
      blitzecdn_edge_runtime: {type: dict, required: true}
      blitzecdn_nginx_http3_listener_owner: {type: str, required: true}
      blitzecdn_nginx_sites: {type: list, elements: dict, required: true}
