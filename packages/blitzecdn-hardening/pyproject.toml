[build-system]
requires = ["hatchling>=1.32.0"]
build-backend = "hatchling.build"

[project]
name = "blitzecdn-hardening"
version = "3.0.0"
description = "Edge host access hardening capability for BlitzeCDN"
readme = "README.md"
requires-python = ">=3.12"
license = { file = "LICENSE" }
dependencies = ["blitzecdn>=3.0.0,<4"]

[project.entry-points."blitzecdn.plugins"]
hardening = "blitzecdn_hardening.plugin"

[tool.hatch.build.targets.wheel]
packages = ["src/blitzecdn_hardening"]

[tool.uv.sources]
blitzecdn = { workspace = true }
