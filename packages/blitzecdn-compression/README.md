# blitzecdn-compression

Optional gzip and Brotli implementation for BlitzeCDN. Install this package to
make the `compression` capability available. The stable `CompressionPolicy`
remains in `blitzecdn`; setting compression to `off` needs no implementation.
