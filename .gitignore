.venv/
.venv.invalid.*/
.ansible/
.cache/

.env
blitzecdn.toml

.state/
generated/*.yml
data/*.yml
data/*.db
data/*.db-shm
data/*.db-wal
**/*.lock
# The fleet lives in the control-plane database now. These are only ever
# present on an installation that predates dynamic inventory; `blitzecdn
# setup` imports the first and renames it to the second.
ansible/inventory/hosts.yml
ansible/inventory/hosts.yml.migrated
# Operator overrides for the edge group. Tracked defaults live beside it in
# defaults.yml; keeping this one untracked is what lets `./install.sh update`
# replace those defaults without colliding with local configuration.
ansible/inventory/group_vars/blitzecdn_edges/local.yml
log/*.log
*.retry

__pycache__/
*.pyc
dist/
*.egg-info/

.mypy_cache/
.pytest_cache/
.ruff_cache/

.coverage
coverage.xml
htmlcov/

.DS_Store
.codex/
